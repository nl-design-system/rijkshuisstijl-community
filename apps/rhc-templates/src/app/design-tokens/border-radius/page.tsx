import PageContent from '@rijkshuisstijl-community/storybook/src/templates/design-tokens/border-radius';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Afrondingen',
  description: 'Design tokens voor afrondingen in de Rijkshuisstijl Community.',
};

export default function Page() {
  return <PageContent />;
}
