---
'@rijkshuisstijl-community/design-tokens': minor
---

Add `rhc.color.light.*` tokens. They're currently a copy of `rhc.color.*` tokens, but in the future the `.rhc.color.*` tokens will automatically switch between light and dark mode, and these tokens will then exist as foundation for dark mode and for folks who need a fixed color palette.
