import { EndItemDirective } from './end-item.directive';

describe('EndItemDirective', () => {
  it('should create an instance', () => {
    const directive = new EndItemDirective();
    expect(directive).toBeTruthy();
  });
});
